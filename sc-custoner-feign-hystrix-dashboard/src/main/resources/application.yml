server:
  port: 8766                    # 指定该服务的端口
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ #指定注册服务地址
spring:
  application:
    name: customer2-feign-hystrix-dashboard
feign:
  hystrix:
    enabled: true
